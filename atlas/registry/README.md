# Global Registry Metadata
#
# File: global_registry.jsonl
# Format: JSONL — one JSON object per line (no comments)
# Version: 1.0.0 · φ-Aligned · 873ms PHI_BEAT
#
# Fields:
#   id          — Unique language number (1-40)
#   code        — Short code (e.g., CPL-L, CIL, TPL)
#   name        — Full name
#   stack       — Stack name
#   stack_number — Roman numeral (I-XI)
#   purpose     — One-line purpose description
#   spec_path   — Path to the .spec file
#   version     — Semantic version
#   phi_sync    — Whether the language is φ-synchronized (873ms heartbeat)
#
# Total: 40 languages across 11 stacks
